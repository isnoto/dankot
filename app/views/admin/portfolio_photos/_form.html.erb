<%= simple_form_for photo do |f| %>
  <%= f.input :image, input_html: { class: 'form-control' } %>
  <%= f.input :category_id, collection: Category.collection,
                            include_blank: false,
                            input_html: { class: 'form-control' } %>
  <%= f.button :submit %>
<% end %>
