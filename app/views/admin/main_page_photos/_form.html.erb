<%= simple_form_for [:admin, photo] do |f| %>
  <%= f.input :image, input_html: { class: 'form-control' } %>
  <%= f.input :position, input_html: { value: @photo.get_position,
                                       class: 'form-control' } %>
  <%= f.button :submit %>
<% end %>
